import React from 'react';


const PokemonCollection: React.FC = (props) => {
    const {pokemon} = props;
    return (
        <div>
            Pokemon Collection
        </div>
    );
};

export default PokemonCollection;
