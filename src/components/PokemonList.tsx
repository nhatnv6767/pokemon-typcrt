import React from 'react';

interface Props {
    name: string,
    id: number,
    image: string,
}

const PokemonList: React.FC<Props> = (props) => {

    return (
        <div>

        </div>
    );
};

export default PokemonList;
